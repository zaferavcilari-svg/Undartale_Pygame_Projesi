nname: Android Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4  # Sadece v4 oldu
    
    - name: Set up Python
      uses: actions/setup-python@v4  # Sadece v4 oldu
      with:
        python-version: '3.x'

    - name: Install Buildozer
      run: pip install buildozer

    - name: Run Buildozer
      run: buildozer android debug

    - name: Upload Artifact
      uses: actions/upload-artifact@v4  # Sadece v4 oldu
      with:
        name: android-build-apk
        path: bin/*.apk
